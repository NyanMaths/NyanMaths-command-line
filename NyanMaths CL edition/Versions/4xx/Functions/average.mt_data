def average (*params):
    "Returs the average of the values which you gave to the function"

    values = list (params)

    summ = 0
    i = len (values) - 1

    while i > -1:
        summ += values[i]
        i -= 1

    return summ / len (values)